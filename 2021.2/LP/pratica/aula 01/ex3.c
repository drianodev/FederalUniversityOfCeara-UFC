#include <stdio.h>

int main(){
    // pode ser: int x = 5, y = 10
    int x, y;
    // pode ser assim tbm
    x = y = 5;
    //x = 5;
    //y = 10;
    printf("x = %d, y = %d", x, y);
    return 0;
}